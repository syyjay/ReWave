# 设置页优化 · 方案 A「入口收敛」（已确认方向）

## 改动 1：重写 settings.html（18 行 → 10 行，基本一屏）
- 保留：账号与订阅卡、外观（2 行）、通用（iCloud / 清缓存 / 语言，3 行）
- 新增「默认设置」组：仅 2 行入口，行内显示当前值——「录音参数 48kHz·24bit·立体声 ›」「导出参数 WAV·320kbps ›」（原 6 行参数收进二级页）
- 「支持与法律」6 行 → 「帮助与支持」2 行：「帮助与反馈」（副标题标注"教程·反馈建议·评分"）、「隐私与条款」
- 「关于 v1.0.0」并入页脚（ReWave v1.0.0 · © 2025 ReWave Studio）

## 改动 2：新增 settings-defaults.html（二级页「默认参数」）
- 导航栏返回「设置」，无 Tab Bar
- 默认录音组：采样率 48kHz / 位深 24bit / 声道 立体声 / 录音格式 M4A（行内值 + chevron）
- 默认导出组：格式 WAV / 比特率 320kbps / 采样率 48kHz
- 底部信息卡：Free 版导出上限 48kHz/16bit，Pro 最高 192kHz/32bit（含升级引导）

## 改动 3：index.html
- 第 04 组新增 settings-defaults.html 的展示卡片（P-021 · 默认参数 · P1）
- 更新该组描述，说明设置页已收敛

## 改动 4：项目文档.md 同步（保持文档-原型对齐）
- 第八部分功能树：Tab 4 设置的「默认录音设置/默认导出设置」收敛为「默认设置（二级页）」；「支持与法律」收敛为「帮助与支持」
- 第十一部分界面清单：新增 P-021「设置·默认参数」行
- 版本摘要补一句设置页信息架构调整

## 验证
- 浏览器截图核对：设置主页一屏内可见（无需大幅滑动）、二级页正常、index 新卡片正常